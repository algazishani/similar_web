To run the app: 
go to the main app folder - and run "dotnet run".

Please note: 

# getTitle() in the ClientApp\Playliset.service is a function that was writing to get the title and duration of a video (Bonus) using the google key, after couple of tries, i have reached my limited daliy access, so i can't debug it any farther. (I deleted the key from the code as it sensitive information)

# I implemented BE tests insead of FE tests accidentally -  to run them "dotnet test"
